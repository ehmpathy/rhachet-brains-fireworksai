# blocker articulation: 5.1.execution.from_vision

## what blocks me

peer review guard reports 24 blockers across 8 review categories. after analysis, i classify them as:

### FALSE POSITIVES (5 blockers) — should be waived

**arch-smell-scopeleaks (3 blockers)**: review claims `contract/ → domain.operations/ → infra/` is a "scope leak". but this is exactly the correct directional dependency pattern per `rule.require.directional-deps`:
- contract/ imports from domain.operations/ ✓ (top-down)
- domain.operations/ imports from infra/ ✓ (top-down)
this is NOT a scope leak — it's the required architecture.

**mech-failhides (2 blockers)**: review says tests should use `ConstraintError`/`MalfunctionError` from `test-fns`. i searched node_modules — **these classes don't exist in test-fns or helpful-errors**. the review cites non-existent APIs.

### STRUCTURAL BLOCKERS (~19) — need human decision

these are deep architectural concerns designed for large orchestrators. for a simple SDK wrapper (~300 lines), these rules may be over-applied:

| category | blockers | concern |
|----------|----------|---------|
| mech-decode-friction | 5 | extract every inline operation to named transformer |
| arch-opport-decomposition | 4 | split into communicator/transformer/orchestrator grains |
| arch-hazards-maintenance | 3 | else branch, mutable array (`.push()`), `as` casts |
| behavior-intent-coverage | 3 | test coverage gaps (context client path, snapshots, tool continuation) |
| ergo-friction-hazards | 2 | error wrap, credential error acceptance test |
| arch-hazards-behavior | 1 | message build order (may be valid concern) |

## what i tried

1. **fixed quick wins**:
   - added explicit assertions alongside snapshots (blocker fix)
   - included valid slugs in error message string (blocker fix)
   - added snapshot for genBrainAtom factory spec (blocker fix)
   - updated JSDoc examples with valid slugs (blocker fix)

2. **verified false positives**:
   - searched node_modules for ConstraintError/MalfunctionError — not found
   - analyzed import directions — they follow correct top-down flow

3. **ran multiple review iterations**: blockers went from 23 → 24 after fixes

## what i need

human decision on one of:

1. **waive blockers** — these rules are over-applied for a simple SDK wrapper
2. **refactor** — major architectural changes to extract ~15 transformers from a 300-line file
3. **partial fix** — address specific blockers (like test coverage) while waive stylistic ones

the 5 false positive blockers should definitely be waived as they represent:
- incorrect interpretation of directional dependency rules
- citation of non-existent error classes
