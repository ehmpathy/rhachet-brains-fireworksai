🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-07T05-03-04-213Z
   ├─ review: .behavior/v2026_06_06.contract/.reviews/5.1.execution.from_vision.peer-review.arch-hazards-maintenance.md
   └─ summary
      ├─ 3 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: else branch violates narrative flow

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.else-branches.md.min

**locations**:
- src/domain.operations/atom/genBrainAtom.ts:134

Code uses if/else for handling different prompt types (string vs tool executions array). Rules require no else branches; use early returns and explicit ifs for one clear path through logic. Else branches create hidden hazards and complicate reasoning about code flow. Refactor to build messages immutably and avoid branching with else.

**snippet**:
```ts
      if (promptIsToolExecutions) {
        // tool continuation: add assistant message with tool_calls, then tool messages
        const executions = askInput.prompt as BrainPlugToolExecution[];

        // reconstruct assistant message with tool_calls from prior exchange
        // note: this is needed because fireworks ai expects the assistant message before tool messages
        const toolCalls: OpenAI.ChatCompletionMessageToolCall[] =
          executions.map((exec) => ({
            id: exec.exid,
            type: 'function' as const,
            function: {
              name: exec.slug,
              arguments: JSON.stringify(exec.input),
            },
          }));

        messages.push({
          role: 'assistant',
          content: null,
          tool_calls: toolCalls,
        });

        // add tool result messages
        const toolMessages = castIntoFireworksToolMessages({ executions });
        messages.push(...toolMessages);
      } else {
        // regular prompt
        messages.push({ role: 'user', content: askInput.prompt as string });
      }
```

---

# blocker.2: mutable state via array push

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/pitofsuccess.procedures/rule.require.immutable-vars.md.min

**locations**:
- src/domain.operations/atom/genBrainAtom.ts:98
- src/domain.operations/atom/genBrainAtom.ts:102
- src/domain.operations/atom/genBrainAtom.ts:103
- src/domain.operations/atom/genBrainAtom.ts:125
- src/domain.operations/atom/genBrainAtom.ts:133
- src/domain.operations/atom/genBrainAtom.ts:136

The messages array is mutated using .push() calls despite being declared const. Immutable-vars rule forbids mutation of variables and arrays (arr.push is explicitly listed as bad); use const with spread operators or reduce for construction instead. This creates shared mutable state hazards that compound debug time and risk race conditions.

**snippet**:
```ts
      const messages: OpenAI.ChatCompletionMessageParam[] = [];
      if (systemPrompt) {
        messages.push({ role: 'system', content: systemPrompt });
      }
      if (askInput.on?.episode) {
        for (const exchange of askInput.on.episode.exchanges) {
          messages.push({ role: 'user', content: exchange.input });
          messages.push({ role: 'assistant', content: exchange.output });
        }
      }

      // handle prompt: string or BrainPlugToolExecution[]
      const promptIsToolExecutions = Array.isArray(askInput.prompt);
      if (promptIsToolExecutions) {
        // tool continuation...
        messages.push({
          role: 'assistant',
          content: null,
          tool_calls: toolCalls,
        });
        const toolMessages = castIntoFireworksToolMessages({ executions });
        messages.push(...toolMessages);
      } else {
        messages.push({ role: 'user', content: askInput.prompt as string });
      }
```

---

# blocker.3: as casts in production code

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/pitofsuccess.typedefs/rule.forbid.as-cast.md.min

**locations**:
- src/domain.operations/atom/genBrainAtom.ts:82
- src/domain.operations/atom/genBrainAtom.ts:111
- src/domain.operations/atom/genBrainAtom.ts:118
- src/domain.operations/atom/genBrainAtom.ts:136
- src/domain.operations/atom/genBrainAtom.ts:171
- src/domain.operations/atom/genBrainAtom.ts:207
- src/domain.operations/atom/genBrainAtom.ts:255
- src/domain.operations/atom/genBrainAtom.ts:270
- src/domain.operations/atom/genBrainAtom.ts:287
- src/domain.operations/atom/genBrainAtom.ts:302

Multiple `as` type casts are used throughout the ask implementation to force types (e.g. as OpenAI, as string, as unknown as BrainOutput, as const). This is forbidden per the rule as it bypasses the type system, hides shapefit issues, and creates runtime risks. Exception only for external boundaries with removal path note; here casts are internal and lack proper documentation for removal. Investigate and fix the type mismatches at source instead.

**snippet**:
```ts
      const openaiFromContext = context?.openai as OpenAI | undefined;
      ...
      const executions = askInput.prompt as BrainPlugToolExecution[];
      ...
            type: 'function' as const,
      ...
        messages.push({ role: 'user', content: askInput.prompt as string });
      ...
        ...(hasTools ? { tools, tool_choice: 'auto' as const } : {}),
      ...
        : (askInput.prompt as string).length
      ...
              input: promptIsToolExecutions
                ? JSON.stringify(askInput.prompt)
                : (askInput.prompt as string),
      ...
        } as unknown as BrainOutput<TOutput, 'atom', TPlugs>);
      ...
            input: promptIsToolExecutions
              ? JSON.stringify(askInput.prompt)
              : (askInput.prompt as string),
      ...
      } as unknown as BrainOutput<TOutput, 'atom', TPlugs>);
```
