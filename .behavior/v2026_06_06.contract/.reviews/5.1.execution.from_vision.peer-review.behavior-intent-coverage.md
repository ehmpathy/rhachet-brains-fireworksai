🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-07T05-06-22-287Z
   ├─ review: .behavior/v2026_06_06.contract/.reviews/5.1.execution.from_vision.peer-review.behavior-intent-coverage.md
   └─ summary
      ├─ 3 blockers 🔴
      └─ 1 nitpicks 🟠

---
# blocker.1: untested code path for openai client from context

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.require.behavior-intent-coverage.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts
- src/domain.operations/atom/genBrainAtom.integration.test.ts

The wish requires adding and proving integration test coverage for the models and behaviors. In genBrainAtom.ts, the ask implementation has a branch to accept an openai client via context (for testing or custom setup) and falls back to env var. However, no test in the integration tests (or unit) exercises passing context with an openai instance. All tests rely exclusively on process.env.FIREWORKS_API_KEY. This leaves the context-provided client code path untested, creating a gap in coverage for the full contract behavior.

**snippet**:
```ts
      const openaiFromContext = context?.openai as OpenAI | undefined;
      if (!openaiFromContext && !process.env.FIREWORKS_API_KEY)
        throw new BadRequestError(
          'FIREWORKS_API_KEY is required when openai client is not provided via context',
          { hint: 'set FIREWORKS_API_KEY env var or pass openai client in context' },
        );
      const openai =
        openaiFromContext ??
        new OpenAI({
          apiKey: process.env.FIREWORKS_API_KEY,
          baseURL: 'https://api.fireworks.ai/inference/v1',
        });
```

---

# blocker.2: positive path outputs lack exhaustive snapshots

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.verification/rule.require.contract-snapshot-exhaustiveness.md

**locations**:
- src/domain.operations/atom/genBrainAtom.integration.test.ts

Every user-faced contract (here the BrainAtom ask method and getBrainAtomsByFireworksAI) must have exhaustive snapshot coverage for positive path, negative path, and edge cases per the rule. Negative/error cases have snapshots (e.g. invalid slug error message). However, all positive ask paths (simple prompt, with briefs, episode continuation, tool invocation/continuation) use ad-hoc assertions like toContain, toBeDefined, and length checks with no snapshots of outputs, full results, or metrics. This creates blind spots for visual diff, drift detection, and regression in PRs.

**snippet**:
```ts
      then('response contains "hello"', () => {
        expect(result.output.content).toBeDefined();
        expect(result.output.content.length).toBeGreaterThan(0);
        expect(result.output.content.toLowerCase()).toContain('hello');
      });

      then('metrics includes token counts', () => {
        expect(result.metrics.size.tokens.input).toBeGreaterThan(0);
        expect(result.metrics.size.tokens.output).toBeGreaterThan(0);
      });
```

---

# blocker.3: incomplete tool continuation coverage for all models

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.require.behavior-intent-coverage.md

**locations**:
- src/domain.operations/atom/genBrainAtom.integration.test.ts

The vision requires proving integration test coverage against all models, with special focus on tooluse. Tool invocation is tested across all 10 models (case9 with repeatably). However, full tool invocation + continuation with executions (to synthesize final answer from tool results) is only exercised for minimax models (case6 using brainAtomWithTools and case11 limited to minimax/2.5 + minimax/2.7). Models like deepseek/v4-pro, kimi/k2.5, glm/5.1 lack the continuation path in tests. This leaves gaps in proving the full tooluse user journey for all supported models.

**snippet**:
```ts
    // test tool use on models that support it
    const modelsToTest: FireworksBrainAtomSlug[] = [
      'fireworks/minimax/2.5',
      'fireworks/minimax/2.7',
    ];

    for (const slug of modelsToTest) {
      when(`[${slug}] tool invocation and continuation`, () => {
        then.repeatably({
          attempts: 3,
          criteria: 'SOME',
        })('tool call + continuation works', async () => {
```

---

# nitpick.1: contract tested as unit instead of acceptance test

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/scope.coverage/rule.require.test-coverage-by-grain.md

**locations**:
- src/contract/sdk/index.unit.test.ts

Per the rule, contracts (user-facing like getBrainAtomsByFireworksAI) require acceptance test + snapshots for visual diff and regression detection. This contract is exposed in src/contract/sdk/index.ts but tested in index.unit.test.ts using unit-style given/when/then and snapshots. No .acceptance.test.ts exists. While snapshots are present, the grain and test type do not match the required acceptance scope for contracts.

**snippet**:
```ts
describe('rhachet-brains-fireworksai.unit', () => {
  given('[case1] getBrainAtomsByFireworksAI', () => {
    when('[t0] called', () => {
      then('returns array with 10 atoms', () => {
        const atoms = getBrainAtomsByFireworksAI();
        expect(atoms).toHaveLength(10);
      });
```
