🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-07T05-00-46-763Z
   ├─ review: .behavior/v2026_06_06.contract/.reviews/5.1.execution.from_vision.peer-review.mech-decode-friction.md
   └─ summary
      ├─ 5 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Inline positional array access in orchestrator

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts

In the ask method (the orchestrator logic composing casts, genBrainContinuables, and openai calls), there is direct `response.choices[0]?.message` access. This is classic decode-friction (array access with positional semantics) that forces readers to mentally simulate the OpenAI response shape instead of grasping intent. Per the rule, this must be extracted to a named transformer (e.g. extractFirstMessage or asFirstChoiceMessage). Orchestrators must read as pure narrative with no inline decode logic.

**snippet**:
```ts
const message = response.choices[0]?.message;
const content = message?.content ?? '';
const toolCalls = message?.tool_calls;
```

---

# blocker.2: Inline map pipeline for tool call reconstruction

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts

The orchestrator contains `executions.map((exec) => ({ id: ..., type: ..., function: ... }))` to build OpenAI tool calls. This is a map pipeline with inline object construction that requires mental simulation of the transformation. It should be extracted to a named transformer (e.g. asFireworksAssistantToolCalls) so the orchestrator line reads as a clear intent call only.

**snippet**:
```ts
const toolCalls: OpenAI.ChatCompletionMessageToolCall[] =
          executions.map((exec) => ({
            id: exec.exid,
            type: 'function' as const,
            function: {
              name: exec.slug,
              arguments: JSON.stringify(exec.input),
            },
          }));
```

---

# blocker.3: Inline complex nested access + type assertion for metrics

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts

The tokensCached extraction uses a type assertion cast plus deep optional chaining on response.usage. This is decode-friction that requires simulating the exact shape and fallback behavior. It must be extracted to a named transformer (e.g. extractCachedTokenCount) to keep the orchestrator narrative clean.

**snippet**:
```ts
const tokensCached =
        (
          response.usage as {
            prompt_tokens_details?: { cached_tokens?: number };
          }
        )?.prompt_tokens_details?.cached_tokens ?? 0;
```

---

# blocker.4: Inline conditional for prompt length calculation

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts

promptLength uses a ternary that branches on promptIsToolExecutions, plus casts and JSON.stringify. This requires mental simulation of the two paths to understand the produced value. Extract to a named transformer like computeInputChars or asPromptLength so the orchestrator does not contain this decode-friction.

**snippet**:
```ts
const promptLength = promptIsToolExecutions
        ? JSON.stringify(askInput.prompt).length
        : (askInput.prompt as string).length;
const charsInput = (systemPrompt?.length ?? 0) + promptLength;
const charsOutput = content.length;
```

---

# blocker.5: Inline map pipeline for tool invocations

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts

In the tool call handling branch, `toolCalls.map((toolCall) => castFromFireworksToolCall({ toolCall }))` is an inline map pipeline inside the orchestrator. Even though the cast is named, the collection transformation logic here forces simulation of the output shape. Extract the mapping to a named transformer for narrative readability.

**snippet**:
```ts
const invocations: BrainPlugToolInvocation[] = toolCalls.map(
          (toolCall) => castFromFireworksToolCall({ toolCall }),
        );
```
