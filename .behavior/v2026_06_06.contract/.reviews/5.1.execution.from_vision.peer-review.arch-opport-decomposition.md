🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-07T05-01-29-756Z
   ├─ review: .behavior/v2026_06_06.contract/.reviews/5.1.execution.from_vision.peer-review.arch-opport-decomposition.md
   └─ summary
      ├─ 4 blockers 🔴
      └─ 2 nitpicks 🟠

---
# blocker.1: Mixed grains: raw i/o and compute inline in ask implementation

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.prefer.decomposable-architecture.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts

The ask method (core of the genBrainAtom domain operation) mixes raw i/o (OpenAI client creation from env or context, and the chat.completions.create call) directly with compute logic (size calculations, token extraction, cost computation) without extracting to named communicators (for SDK i/o) or transformers (for pure compute like size/cost). This prevents recomposition and makes the operation non-narrative. Orchestrators must compose leaf operations; leafs must encapsulate their grain.

**snippet**:
```ts
      const openaiFromContext = context?.openai as OpenAI | undefined;
      if (!openaiFromContext && !process.env.FIREWORKS_API_KEY)
        throw new BadRequestError(
          'FIREWORKS_API_KEY is required when openai client is not provided via context',
          { hint: 'set FIREWORKS_API_KEY env var or pass openai client in context' },
        );
      const openai =
        openaiFromContext ??
        new OpenAI({
          apiKey: process.env.FIREWORKS_API_KEY,
          baseURL: 'https://api.fireworks.ai/inference/v1',
        });

      // ... later ...
      const response = await openai.chat.completions.create({
```

---

# blocker.2: Decode-friction in orchestrator: inline message building logic

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.prefer.decomposable-architecture.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts

The ask implementation (orchestrator for the atom workflow) contains inline for-loop and conditional pushes to build the messages array from episode exchanges and system prompt. This requires mental simulation of 'how' the prompt is assembled instead of delegating to a named transformer like 'buildMessagesFromEpisode' or 'castEpisodeToOpenAIMessages'. Orchestrators must read as prose with each line telling 'what'.

**snippet**:
```ts
      const messages: OpenAI.ChatCompletionMessageParam[] = [];
      if (systemPrompt) {
        messages.push({ role: 'system', content: systemPrompt });
      }
      if (askInput.on?.episode) {
        for (const exchange of askInput.on.episode.exchanges) {
          messages.push({ role: 'user', content: exchange.input });
          messages.push({ role: 'assistant', content: exchange.output });
        }
      }
```

---

# blocker.3: Unclear grain + decode-friction: prompt type branching and tool reconstruction logic

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.prefer.decomposable-architecture.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts

Inside ask, a local 'promptIsToolExecutions' boolean + if/else + map + casts handles switching between normal string prompt and BrainPlugToolExecution[] continuation. This ambiguous logic (part compute, part i/o prep) is mixed with the communicator call and requires simulation to understand the branching and reconstruction of tool_calls. Should be split into clear transformer (e.g. castPromptToMessages) and communicator (e.g. sdkFireworks.ask).

**snippet**:
```ts
      const promptIsToolExecutions = Array.isArray(askInput.prompt);
      if (promptIsToolExecutions) {
        const executions = askInput.prompt as BrainPlugToolExecution[];
        const toolCalls: OpenAI.ChatCompletionMessageToolCall[] =
          executions.map((exec) => ({
            id: exec.exid,
            type: 'function' as const,
            function: {
              name: exec.slug,
              arguments: JSON.stringify(exec.input),
            },
          }));
        messages.push({
          role: 'assistant',
          content: null,
          tool_calls: toolCalls,
        });
        const toolMessages = castIntoFireworksToolMessages({ executions });
        messages.push(...toolMessages);
      } else {
        messages.push({ role: 'user', content: askInput.prompt as string });
      }
```

---

# blocker.4: Decode-friction: inline size/metrics computation with type casts

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.prefer.decomposable-architecture.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts

After the API response, multiple lines of inline computation extract tokens, compute chars, cast usage details, and build size object. This is pure transformer logic embedded in the operation body instead of extracted to e.g. computeBrainOutputSize({ response, prompt }). Violates single-responsibility per grain and makes the flow non-narrative.

**snippet**:
```ts
      const tokensInput = response.usage?.prompt_tokens ?? 0;
      const tokensOutput = response.usage?.completion_tokens ?? 0;
      const tokensCached =
        (
          response.usage as {
            prompt_tokens_details?: { cached_tokens?: number };
          }
        )?.prompt_tokens_details?.cached_tokens ?? 0;

      const promptLength = promptIsToolExecutions
        ? JSON.stringify(askInput.prompt).length
        : (askInput.prompt as string).length;
      const charsInput = (systemPrompt?.length ?? 0) + promptLength;
      const charsOutput = content.length;
```

---

# nitpick.1: Duplicated continuables construction logic

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.prefer.decomposable-architecture.md

**locations**:
- src/domain.operations/atom/genBrainAtom.ts

Nearly identical blocks calling genBrainContinuables appear for the tool-calls path and the normal path (differing only in input/output strings). This duplication is in the same operation; while wet-over-dry prefers waiting for reuse, the pattern is already repeated twice inside one function and should be extracted to a small transformer to improve recomposability and reduce decode-friction.

**snippet**:
```ts
      const { episode, series } = await genBrainContinuables({
        for: { grain: 'atom' },
        on: { episode: askInput.on?.episode ?? null, series: null },
        with: {
          exchange: {
            input: promptIsToolExecutions
              ? JSON.stringify(askInput.prompt)
              : (askInput.prompt as string),
            output: JSON.stringify(toolCalls),
            exid: response.id ?? null,
          },
          episode: { exid: response.id ?? null },
        },
      });
```

---

# nitpick.2: Hardcoded list of gen calls without decomposition

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.prefer.decomposable-architecture.md

**locations**:
- src/contract/sdk/index.ts

getBrainAtomsByFireworksAI returns a literal array of 10 direct genBrainAtom calls. While small, this is an orchestrator that hardcodes the composition instead of delegating to a named transformer (e.g. getAllFireworksBrainAtomSlugs() + map to gen). Minor but reduces recomposition potential if the list needs to evolve.

**snippet**:
```ts
export const getBrainAtomsByFireworksAI = (): BrainAtom[] => {
  return [
    genBrainAtom({ slug: 'fireworks/qwen3.6/plus' }),
    genBrainAtom({ slug: 'fireworks/deepseek/v4-pro' }),
    genBrainAtom({ slug: 'fireworks/deepseek/v4-flash' }),
    genBrainAtom({ slug: 'fireworks/kimi/k2.5' }),
    genBrainAtom({ slug: 'fireworks/kimi/k2.6' }),
    genBrainAtom({ slug: 'fireworks/glm/5.1' }),
    genBrainAtom({ slug: 'fireworks/minimax/2.5' }),
    genBrainAtom({ slug: 'fireworks/minimax/2.7' }),
    genBrainAtom({ slug: 'fireworks/gpt-oss/120b' }),
    genBrainAtom({ slug: 'fireworks/gpt-oss/20b' }),
  ];
};
```
