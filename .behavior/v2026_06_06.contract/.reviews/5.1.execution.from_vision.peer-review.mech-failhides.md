🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-07T04-59-16-991Z
   ├─ review: .behavior/v2026_06_06.contract/.reviews/5.1.execution.from_vision.peer-review.mech-failhides.md
   └─ summary
      ├─ 2 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Test uses incorrect error class for absent resource

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/pitofsuccess.errors/rule.require.failloud.md

**locations**:
- src/domain.operations/atom/genBrainAtom.integration.test.ts:22

Per the test failloud rule, tests must throw ConstraintError (from 'test-fns') for caller-fixable issues like missing API keys (exit code 2), not BadRequestError. The current code uses BadRequestError from 'helpful-errors' (intended for production code per other rules) which violates the test-specific requirement for proper classes, exit codes, and consistent test error handling. This is a blocker as it uses the wrong class for test setup.

**snippet**:
```ts
if (!process.env.FIREWORKS_API_KEY)
  throw new BadRequestError(
    'FIREWORKS_API_KEY is required for integration tests',
    { hint: 'run: rhx keyrack unlock --owner ehmpath --env test', env: 'FIREWORKS_API_KEY' },
  );
```

---

# blocker.2: Test uses incorrect error class for unexpected state

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/pitofsuccess.errors/rule.require.failloud.md

**locations**:
- src/domain.operations/atom/genBrainAtom.integration.test.ts:262
- src/domain.operations/atom/genBrainAtom.integration.test.ts:316
- src/domain.operations/atom/genBrainAtom.integration.test.ts:356
- src/domain.operations/atom/genBrainAtom.integration.test.ts:490

Per the test failloud and failfast rules, tests must use MalfunctionError (from 'test-fns', exit code 1) for server-fixable unexpected states (e.g. missing tool invocations in test flows), not UnexpectedCodePathError from 'helpful-errors'. Multiple instances of this pattern exist. Using the wrong class violates the requirement for proper error classes with exit code semantics in tests. This is a blocker.

**snippet**:
```ts
const invocation = resultFirst.calls?.tools?.[0];
if (!invocation)
  throw new UnexpectedCodePathError('no tool invocation found', {
    resultFirst,
  });
```
