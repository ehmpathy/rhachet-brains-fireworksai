# verification checklist

## behavior coverage

the wish requested API key injection via context, per xai pattern.

| behavior | test file | status |
|----------|-----------|--------|
| keyrack shorthand pattern | genBrainAtom.credentials.integration.test.ts case5 | ✓ directly tested |
| explicit getter pattern | genBrainAtom.credentials.integration.test.ts case1 | ✓ |
| getter called fresh per ask | genBrainAtom.credentials.integration.test.ts case2 | ✓ |
| getter error propagation | genBrainAtom.credentials.integration.test.ts case3 | ✓ |
| multi-tenant isolation | genBrainAtom.credentials.integration.test.ts case4 | ✓ |
| env fallback | genBrainAtom.integration.test.ts (all 10 models) | ✓ |

## zero skips verified

- [x] no .skip() or .only() found
- [x] no silent credential bypasses
- [x] no prior failures carried forward

## snapshot coverage for contract outputs

| contract | output variants | snapshot file | status |
|----------|-----------------|---------------|--------|
| getBrainAtomsByFireworksAI | slugs, specs | index.unit.test.ts.snap | ✓ |
| genBrainAtom factory | spec, invalid slug error | index.unit.test.ts.snap | ✓ |
| genBrainAtom.ask | tool use error | genBrainAtom.integration.test.ts.snap | ✓ |

checklist:
- [x] every new cli command has `.snap` snapshots → n/a (no cli)
- [x] every new app screen has `.snap` snapshots → n/a (no app)
- [x] every new sdk method has `.snap` snapshots → covered
- [x] each output variant is exercised → covered
- [x] snapshots demonstrate actual output → verified

## snapshot change rationalization

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| (no changes) | n/a | n/a | n/a |

checklist:
- [x] every `.snap` change has been reviewed → no changes
- [x] intended changes have clear rationale → n/a
- [x] accidental changes have been reverted → n/a

## tests executed — with proof

| test suite | command run | result | proof |
|------------|-------------|--------|-------|
| types | `rhx git.repo.test --what types` | ✓ | exit 0, passed 4s |
| lint | `rhx git.repo.test --what lint` | ✓ | exit 0, passed 3s |
| format | `rhx git.repo.test --what format` | ✓ | exit 0, passed 0s |
| unit | `rhx git.repo.test --what unit --mode apply` | ✓ | exit 0, 10 tests passed, 2s |
| integration | `rhx git.repo.test --what integration --mode apply` | ✓ | exit 0, 55 tests passed, 165s |

checklist:
- [x] every test command was run
- [x] every test command output was observed
- [x] the exact exit code was verified
- [x] no tests were skipped, mocked, or faked

## fixes applied at verification

1. **lint failure: unused devDependency**
   - `rhachet-brains-xai` was flagged as unused
   - fix: `npm remove rhachet-brains-xai`
   - rationale: package was installed for reference only, not used in code

2. **integration test timeout: kimi/k2.5 model (briefs)**
   - kimi/k2.5 timed out at 30s (exceeded jest default timeout)
   - fix: increased jest.setTimeout from 30000 to 60000 in genBrainAtom.integration.test.ts
   - rationale: kimi/k2.5 is slower on Fireworks infrastructure than other models (~45s vs ~1-3s)

3. **integration test timeout: kimi/k2.5 model (tool use)**
   - kimi/k2.5 tool use timed out at 60s on verification re-run
   - fix: increased jest.setTimeout from 60000 to 90000
   - rationale: tool use calls are slower than briefs (~60s+ vs ~50s)

## contract output snapshot exhaustiveness

| contract type | contract | positive path | negative path | edge cases |
|---------------|----------|---------------|---------------|------------|
| sdk | getBrainAtomsByFireworksAI | ✓ slugs + specs | n/a | n/a |
| sdk | genBrainAtom | ✓ valid slug | ✓ invalid slug | ✓ |
| sdk | genBrainAtom.ask | ✓ response | ✓ tool use error | ✓ |

checklist:
- [x] every sdk method has return value snapshots
- [x] every contract has edge case snapshots

## blockers

- none
