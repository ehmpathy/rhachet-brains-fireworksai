# blocker: 5.3.verification

## what blocks

peer reviews are configured to use brain `xai/grok/code-fast-1` which is not available. all 7 peer reviews malfunctioned with:

```
BrainChoiceNotFoundError: brain not found: xai/grok/code-fast-1
```

## what was tried

1. completed all 11 self-reviews successfully:
   - r1-r3: has-behavior-coverage → has-all-tests-passed
   - r4-r5: has-journey-tests-from-repros (keyrack shorthand test added)
   - r5-r7: has-contract-output-variants-snapped
   - r6-r7: has-snap-changes-rationalized
   - r7-r8: has-critical-paths-frictionless
   - r8-r9: has-ergonomics-validated
   - r9-r10: has-play-test-convention
   - r10-r11: has-fixed-all-gaps

2. all self-reviews documented in `.behavior/v2026_06_11.fix-apikeys/review/self/`

3. signaled `--as passed` which triggered guard

4. guard ran 7 peer reviews, all malfunctioned

5. judge allowed passage despite malfunctions

## what is needed

human intervention to fix reviewer brain configuration:

- **current**: `xai/grok/code-fast-1` (not available)
- **needed**: an available anthropic/claude brain (e.g., `anthropic/claude/sonnet`)

the reviewer brain configuration is in infrastructure, not in this repo's code.

## note

this is not a code defect. all verification work is complete:
- 56 tests pass (types, lint, format, unit, integration)
- all 6 credential journeys tested
- snapshots clean and intentional
- no TODO/LATER items
- keyrack shorthand gap fixed (case5 added)

the behavior is ready for merge once peer reviews can execute.
